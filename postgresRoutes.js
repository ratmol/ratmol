const express = require('express');
const router = express.Router();

// import your model
const PostgresModel = require('../models/postgresModel');

// Create a new item

// Other CRUD operations (read, update, delete) ...

module.exports = router;